This folder is used for static assets such as icons and splash images.

Add files named `icon.png`, `splash.png`, and `adaptive-icon.png` if you want custom icons/splash screens, or update `app.json` to remove or reference different files.
